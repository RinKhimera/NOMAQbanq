# Campagne audit 2026-07 (C4→C7) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Livrer les 4 campagnes restantes de l'audit 2026-07 (C4 fiabilité paiements, C5 nettoyage, C6 refactors de duplication, C7 frontend perf/a11y/SEO) sur la branche `campaign/audit-2026-07-c4-c7`, en une PR unique fermant #111 #112 #113 #114.

**Architecture:** Cœur C4 = une fonction unique `recomputeAccess` qui dérive `user_access` des transactions `completed` (source de vérité), appelée sur toute transition de statut et avant suppression, sous le même verrou `FOR UPDATE` que l'existant. C6 = fusions mécaniques iso-comportement. C7 = bascule des stats marketing en RSC + corrections a11y/SEO ciblées.

**Tech Stack:** Next.js 16 (App Router), React 19, TypeScript, Drizzle + Neon, Better Auth, Vitest (`bun run test`, JAMAIS `bun test`), Playwright.

**Spec:** `docs/superpowers/specs/2026-07-16-audit-2026-07-c4-c7-design.md`

---

## Conventions d'exécution (s'appliquent à toutes les tâches)

- Branche : `campaign/audit-2026-07-c4-c7` (déjà créée ; contient le spec + un commit chore). **Jamais de commit sur `main`.**
- Gate par tâche : `bun run check` (prettier + tsc + eslint) et `bun run test` verts AVANT chaque commit.
- Tests d'intégration : `bun run test:integration` (crée/migre/détruit une branche Neon ; `--keep` pour déboguer). Lance TOUTE la suite intégration — compter quelques minutes.
- Messages de commit : préfixe campagne + issue, ex. `C4 (#111): ...`, suffixe `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Toute suppression de fichier est précédée d'un grep de références (commande donnée dans la tâche).
- Texte UI en **français avec accents**.

---

## Tâche 1 : C5 — Nettoyage sans risque (#112)

**Files:**

- Modify: `package.json` (deps `svix`, `@trivago/prettier-plugin-sort-imports`)
- Delete: `tailwind.config.ts` + Modify: `components.json:7`
- Delete: `components/shared/scroll-to-top.tsx` + Modify: `vitest.config.ts:71`
- Delete: `scripts/migrate-media-to-s3.ts`
- Modify: `AGENTS.md` (note Icons)

- [ ] **Étape 1.1 : Vérifier l'absence de références résiduelles** (déjà pré-vérifié au design, re-confirmer avant suppression)

```powershell
# svix : ne doit matcher QUE package.json / bun.lock / le spec de design
Select-String -Path (Get-ChildItem -Recurse -Include *.ts,*.tsx -Exclude node_modules | % FullName) -Pattern "svix" -SimpleMatch
# scroll-to-top : ne doit matcher QUE le fichier lui-même + vitest.config.ts
# migrate-media-to-s3 : ne doit matcher QUE le fichier lui-même + docs/
```

Attendu : aucune importation applicative. Si un import apparaît, STOP et re-évaluer l'item.

- [ ] **Étape 1.2 : Retirer svix et déplacer le plugin prettier en devDependencies**

```powershell
bun remove svix
bun remove @trivago/prettier-plugin-sort-imports
bun add -d @trivago/prettier-plugin-sort-imports
```

- [ ] **Étape 1.3 : Supprimer `tailwind.config.ts` et corriger `components.json`**

Supprimer le fichier, puis dans `components.json` ligne 7, remplacer :

```json
    "config": "tailwind.config.ts",
```

par (convention shadcn pour Tailwind v4 — pas de fichier config) :

```json
    "config": "",
```

- [ ] **Étape 1.4 : Supprimer `components/shared/scroll-to-top.tsx` et son exclusion coverage**

Supprimer le fichier, puis retirer la ligne `"components/shared/scroll-to-top.tsx",` de `vitest.config.ts:71` (liste `coverage.exclude`).

- [ ] **Étape 1.5 : Supprimer `scripts/migrate-media-to-s3.ts`** (migration one-shot déjà exécutée ; aucun script package.json ne le référence — seul `audit:medias` → `audit-medias.ts` reste, on n'y touche pas).

- [ ] **Étape 1.6 : Corriger la note Icons d'AGENTS.md** (comptage réel : lucide-react = 153 fichiers, tabler = 46). Remplacer :

```markdown
- **Icons** : `@tabler/icons-react` (primaire), `lucide-react` (secondaire)
```

par :

```markdown
- **Icons** : `lucide-react` (primaire — utilisé partout), `@tabler/icons-react` (secondaire — surtout admin/dashboard et profil)
```

- [ ] **Étape 1.7 : Gate + commit**

```powershell
bun run check && bun run test
git add -A
git commit -m "C5 (#112): nettoyage — svix, tailwind.config fantôme, scroll-to-top, script one-shot, note icônes"
```

Attendu : check vert (prettier reformatera package.json trié), tests verts.

---

## Tâche 2 : C4 — Tests d'intégration `recomputeAccess` (rouge d'abord)

**Files:**

- Modify: `tests/integration/payments-manual.test.ts` (remplace le `describe("revokeAccessIfLast")`, lignes 166-208, et l'import ligne 5-9)

Contexte pour l'ingénieur : `recomputeAccess(tx, { userId, excludeTransactionId? })` (Tâche 3) reconstruit `user_access` par type d'accès = `max(accessExpiresAt)` des transactions `completed` couvrant ce type (`accessType` égal OU produit combo), pointe `lastTransactionId` sur la transaction porteuse du max, supprime la ligne si aucune transaction ne couvre le type, et retourne `{ accessReducedOrRemoved: boolean }`.

- [ ] **Étape 2.1 : Remplacer l'import et le bloc de tests**

Dans l'import (lignes 5-9), remplacer `revokeAccessIfLast` par `recomputeAccess`. Ajouter aux ids `users` (lignes 28-35) : `recompute: createId(), recredit: createId(), reminder: createId(),` (conserver les existants ; `revoke` et `comboDelete` restent utilisés ci-dessous). Remplacer intégralement le `describe("revokeAccessIfLast", ...)` par :

```ts
const setStatus = (txId: string, status: "completed" | "refunded") =>
  db.update(transactions).set({ status }).where(eq(transactions.id, txId))

const readAccessRow = async (userId: string, type: "exam" | "training") => {
  const [row] = await db
    .select({
      expiresAt: userAccess.expiresAt,
      lastTransactionId: userAccess.lastTransactionId,
      expiryReminderSentAt: userAccess.expiryReminderSentAt,
    })
    .from(userAccess)
    .where(and(eq(userAccess.userId, userId), eq(userAccess.accessType, type)))
    .limit(1)
  return row ?? null
}

const recompute = (userId: string, excludeTransactionId?: string) =>
  db.transaction((tx) => recomputeAccess(tx, { userId, excludeTransactionId }))

describe("recomputeAccess", () => {
  it("refund de la DERNIÈRE transaction : restaure l'expiration précédente (pas de perte du cumul)", async () => {
    const txA = await grant(users.recompute, pExam) // ~ +30 j
    const txB = await grant(users.recompute, pExam) // cumul → ~ +60 j
    expect(daysFromNow(await readExpiry(users.recompute, "exam"))).toBe(60)

    await setStatus(txB, "refunded")
    const result = await recompute(users.recompute)

    expect(result.accessReducedOrRemoved).toBe(true)
    const row = await readAccessRow(users.recompute, "exam")
    expect(daysFromNow(row?.expiresAt ?? null)).toBe(30) // restauré, PAS supprimé
    expect(row?.lastTransactionId).toBe(txA) // re-pointé sur la transaction restante
  })

  it("refund d'une transaction NON dernière : l'accès ne bouge pas", async () => {
    // État hérité du test précédent : txA (+30) est la seule complétée.
    const txC = await grant(users.recompute, pExam) // re-cumule → ~ +60 j
    const before = await readAccessRow(users.recompute, "exam")

    // On re-refund une transaction qui n'est pas porteuse du max : no-op.
    const [nonLast] = await db
      .select({ id: transactions.id })
      .from(transactions)
      .where(
        and(
          eq(transactions.userId, users.recompute),
          eq(transactions.status, "refunded"),
        ),
      )
      .limit(1)
    const result = await recompute(users.recompute)
    expect(result.accessReducedOrRemoved).toBe(false)
    const after = await readAccessRow(users.recompute, "exam")
    expect(after?.expiresAt.getTime()).toBe(before?.expiresAt.getTime())
    expect(after?.lastTransactionId).toBe(txC)
    expect(nonLast).toBeDefined() // sanity : il existe bien une refunded en base
  })

  it("refund de la transaction UNIQUE : supprime la ligne d'accès", async () => {
    const txId = await grant(users.revoke, pExam)
    await setStatus(txId, "refunded")
    const result = await recompute(users.revoke)
    expect(result.accessReducedOrRemoved).toBe(true)
    expect(await readExpiry(users.revoke, "exam")).toBeNull()
  })

  it("refunded → completed : RE-CRÉDITE l'accès (bug #111-1)", async () => {
    const txId = await grant(users.recredit, pExam)
    await setStatus(txId, "refunded")
    await recompute(users.recredit)
    expect(await readExpiry(users.recredit, "exam")).toBeNull()

    await setStatus(txId, "completed")
    const result = await recompute(users.recredit)
    expect(result.accessReducedOrRemoved).toBe(false)
    const row = await readAccessRow(users.recredit, "exam")
    expect(daysFromNow(row?.expiresAt ?? null)).toBe(30)
    expect(row?.lastTransactionId).toBe(txId)
  })

  it("combo : le recompute couvre exam ET training, et la suppression passe la FK restrict", async () => {
    const txId = await grant(users.comboDelete, pCombo)
    expect(daysFromNow(await readExpiry(users.comboDelete, "exam"))).toBe(90)
    expect(daysFromNow(await readExpiry(users.comboDelete, "training"))).toBe(
      90,
    )

    await db.transaction(async (tx) => {
      const result = await recomputeAccess(tx, {
        userId: users.comboDelete,
        excludeTransactionId: txId,
      })
      expect(result.accessReducedOrRemoved).toBe(true)
      // Ne doit PAS lever de violation de clé étrangère (lastTransactionId restrict).
      await tx.delete(transactions).where(eq(transactions.id, txId))
    })

    expect(await readExpiry(users.comboDelete, "exam")).toBeNull()
    expect(await readExpiry(users.comboDelete, "training")).toBeNull()
  })

  it("est idempotent : deux recomputes successifs produisent le même état", async () => {
    const before = await readAccessRow(users.recompute, "exam")
    const r1 = await recompute(users.recompute)
    const r2 = await recompute(users.recompute)
    expect(r1.accessReducedOrRemoved).toBe(false)
    expect(r2.accessReducedOrRemoved).toBe(false)
    const after = await readAccessRow(users.recompute, "exam")
    expect(after?.expiresAt.getTime()).toBe(before?.expiresAt.getTime())
    expect(after?.lastTransactionId).toBe(before?.lastTransactionId)
  })

  it("expiryReminderSentAt : conservé quand l'accès est RÉDUIT, ré-armé quand il est PROLONGÉ", async () => {
    const txA = await grant(users.reminder, pExam) // +30
    const txB = await grant(users.reminder, pExam) // +60
    const sentAt = new Date()
    await db
      .update(userAccess)
      .set({ expiryReminderSentAt: sentAt })
      .where(eq(userAccess.userId, users.reminder))

    // Réduction (refund de txB) : le rappel déjà envoyé reste marqué.
    await setStatus(txB, "refunded")
    await recompute(users.reminder)
    let row = await readAccessRow(users.reminder, "exam")
    expect(row?.expiryReminderSentAt).not.toBeNull()

    // Prolongation (txB re-complétée) : le rappel est ré-armé (null).
    await setStatus(txB, "completed")
    await recompute(users.reminder)
    row = await readAccessRow(users.reminder, "exam")
    expect(row?.expiryReminderSentAt).toBeNull()
    expect(row?.lastTransactionId).toBe(txB)
    expect(txA).toBeDefined()
  })
})
```

Note : `grant()` cumule sur l'expiration existante (comportement testé lignes 146-150) — les attendus 30/60/90 jours découlent de `durationDays` des produits déjà seedés dans ce fichier.

- [ ] **Étape 2.2 : Vérifier que la suite échoue (rouge)**

```powershell
bun run test:integration
```

Attendu : ÉCHEC — `recomputeAccess` n'existe pas encore (`has no exported member`). Les autres fichiers d'intégration restent verts.

- [ ] **Étape 2.3 : Commit des tests** (rouge assumé, la Tâche 3 suit immédiatement)

```powershell
git add tests/integration/payments-manual.test.ts
git commit -m "C4 (#111): tests d'intégration recomputeAccess (rouge)"
```

---

## Tâche 3 : C4 — Implémentation `recomputeAccess` (vert)

**Files:**

- Modify: `features/payments/lib.ts` (supprime `revokeAccessIfLast` lignes 134-167, ajoute `bestCoveringTransaction` + `recomputeAccess`)

- [ ] **Étape 3.1 : Ajouter les imports nécessaires**

Ligne 1 de `features/payments/lib.ts`, étendre l'import drizzle et le schéma :

```ts
import { and, desc, eq, ne, or } from "drizzle-orm"
import "server-only"
import { db } from "@/db"
import { products, transactions, user, userAccess } from "@/db/schema"
import { createId } from "@/lib/ids"
```

- [ ] **Étape 3.2 : Remplacer `revokeAccessIfLast` (lignes 134-167) par le bloc suivant**

```ts
/** Accepte le handle de transaction OU l'instance db (lectures hors transaction). */
type DbLike = Tx | typeof db

/**
 * Meilleure transaction `completed` couvrant ce type d'accès pour cet utilisateur :
 * `accessType` égal OU produit combo (un combo couvre exam ET training), expiration
 * la plus tardive. `excludeTransactionId` écarte une transaction en cours de
 * suppression. Tri secondaire (createdAt, id) pour un choix déterministe à égalité.
 */
export const bestCoveringTransaction = async (
  dbOrTx: DbLike,
  userId: string,
  accessType: "exam" | "training",
  excludeTransactionId?: string,
): Promise<{ id: string; accessExpiresAt: Date } | null> => {
  const rows = await dbOrTx
    .select({
      id: transactions.id,
      accessExpiresAt: transactions.accessExpiresAt,
    })
    .from(transactions)
    .innerJoin(products, eq(products.id, transactions.productId))
    .where(
      and(
        eq(transactions.userId, userId),
        eq(transactions.status, "completed"),
        or(eq(transactions.accessType, accessType), eq(products.isCombo, true)),
        excludeTransactionId
          ? ne(transactions.id, excludeTransactionId)
          : undefined,
      ),
    )
    .orderBy(
      desc(transactions.accessExpiresAt),
      desc(transactions.createdAt),
      desc(transactions.id),
    )
    .limit(1)
  return rows[0] ?? null
}

export type RecomputeAccessResult = {
  /** true si au moins un type d'accès a été supprimé ou raccourci. */
  accessReducedOrRemoved: boolean
}

/**
 * Reconstruit `user_access` (exam ET training) depuis les transactions `completed`
 * de l'utilisateur — les transactions sont la source de vérité. À appeler DANS
 * `db.transaction`, sur TOUTE transition de statut (`completed ↔ refunded`) et
 * AVANT la suppression d'une transaction (avec `excludeTransactionId`, sinon la FK
 * `user_access.last_transaction_id` — NOT NULL, onDelete: restrict — bloque le DELETE).
 * Verrou `user FOR UPDATE` : parité avec `grantManualAccess`/`completeStripeTransaction`.
 *
 * Sémantique : l'expiration restaurée est `max(accessExpiresAt)` des transactions
 * restantes — c.-à-d. l'expiration telle qu'elle était AVANT la transaction retirée
 * (chaque transaction porte le snapshot du cumul à son octroi). On ne re-simule PAS
 * le cumul « comme si la transaction n'avait jamais existé ».
 *
 * `expiryReminderSentAt` : ré-armé (null) uniquement si l'expiration avance —
 * même règle que `grantManualAccess`.
 */
export async function recomputeAccess(
  tx: Tx,
  params: { userId: string; excludeTransactionId?: string },
): Promise<RecomputeAccessResult> {
  const { userId, excludeTransactionId } = params

  const [lockedUser] = await tx
    .select({ id: user.id })
    .from(user)
    .where(eq(user.id, userId))
    .for("update")
  if (!lockedUser) throw new Error("USER_NOT_FOUND")

  let accessReducedOrRemoved = false

  for (const accessType of ["exam", "training"] as const) {
    const existing = await readAccess(tx, userId, accessType)
    const best = await bestCoveringTransaction(
      tx,
      userId,
      accessType,
      excludeTransactionId,
    )

    if (!best) {
      if (existing) {
        accessReducedOrRemoved = true
        await tx
          .delete(userAccess)
          .where(
            and(
              eq(userAccess.userId, userId),
              eq(userAccess.accessType, accessType),
            ),
          )
      }
      continue
    }

    const extended =
      !existing || best.accessExpiresAt.getTime() > existing.expiresAt.getTime()
    if (
      existing &&
      best.accessExpiresAt.getTime() < existing.expiresAt.getTime()
    )
      accessReducedOrRemoved = true

    await tx
      .insert(userAccess)
      .values({
        userId,
        accessType,
        expiresAt: best.accessExpiresAt,
        lastTransactionId: best.id,
      })
      .onConflictDoUpdate({
        target: [userAccess.userId, userAccess.accessType],
        set: {
          expiresAt: best.accessExpiresAt,
          lastTransactionId: best.id,
          // Re-arme le rappel de fin d'accès uniquement si l'accès est prolongé.
          ...(extended ? { expiryReminderSentAt: null } : {}),
        },
      })
  }

  return { accessReducedOrRemoved }
}
```

Note : `readAccess` (lignes 19-30) existe déjà et renvoie `{ expiresAt, lastTransactionId }`. `grantManualAccess` ne change PAS.

- [ ] **Étape 3.3 : Corriger la compilation des appelants** — `features/payments/actions.ts` importe encore `revokeAccessIfLast` (ligne 24) : c'est l'objet de la Tâche 4, à faire AVANT de lancer les tests. Enchaîner directement.

---

## Tâche 4 : C4 — Brancher les actions sur `recomputeAccess`

**Files:**

- Modify: `features/payments/actions.ts:24` (import), `:201-206` (update), `:264-269` (delete)

- [ ] **Étape 4.1 : Remplacer l'import (ligne 24)**

```ts
import { grantManualAccess, recomputeAccess } from "./lib"
```

- [ ] **Étape 4.2 : `updateManualTransaction` — recomputer sur TOUTE transition de statut**

Remplacer le bloc lignes 201-217 (l'`if refunded` + l'`update`) par :

```ts
await tx
  .update(transactions)
  .set({
    amountPaid: data.amountPaid,
    currency: data.currency,
    paymentMethod: data.paymentMethod,
    notes: data.notes ?? null,
    ...(data.status ? { status: data.status } : {}),
  })
  .where(eq(transactions.id, data.transactionId))

// Toute transition de statut (completed ↔ refunded) rejoue le calcul d'accès :
// couvre la révocation ET le re-crédit (bug refunded → completed, #111).
if (data.status && data.status !== transaction.status) {
  await recomputeAccess(tx, { userId: transaction.userId })
}
```

(L'update passe AVANT le recompute : `recomputeAccess` lit les statuts committés dans la transaction en cours.)

- [ ] **Étape 4.3 : `deleteManualTransaction` — recomputer en excluant la transaction, PUIS supprimer**

Remplacer les lignes 264-269 (`const revoked = await revokeAccessIfLast(...)` + delete) par :

```ts
const { accessReducedOrRemoved } = await recomputeAccess(tx, {
  userId: transaction.userId,
  excludeTransactionId: transaction.id,
})
await tx.delete(transactions).where(eq(transactions.id, transactionId))
return accessReducedOrRemoved
```

(La variable de retour du callback s'appelait `revoked` → le `return revoked` ligne 269 est couvert par le `return` ci-dessus ; le nom exposé `accessRevoked` de l'action ne change pas.)

- [ ] **Étape 4.4 : Vérifier compilation + tests d'intégration (vert)**

```powershell
bun run check
bun run test:integration
```

Attendu : tout vert, y compris les 7 nouveaux tests `recomputeAccess`.

- [ ] **Étape 4.5 : Commit**

```powershell
git add features/payments/lib.ts features/payments/actions.ts
git commit -m "C4 (#111): recomputeAccess — l'accès dérive des transactions (refund/re-crédit/suppression)"
```

---

## Tâche 5 : C4 — `AccessImpact.restoredExpiresAt` (avertissements honnêtes)

Avec le recompute, « révoquera l'accès » devient parfois faux (l'accès est _ramené_ à l'échéance précédente). Les deux dialogs admin doivent le dire.

**Files:**

- Modify: `features/payments/dal.ts:493-540` (type + `getTransactionAccessImpact`)
- Modify: `components/shared/payments/edit-transaction-modal.tsx:482-503`
- Modify: `components/shared/payments/delete-transaction-dialog.tsx:84,134` (bloc d'avertissement)

- [ ] **Étape 5.1 : Étendre le type et le DAL**

Dans `features/payments/dal.ts`, remplacer le type (lignes 493-498) par :

```ts
export type AccessImpact = {
  willRevokeAccess: boolean
  /** Epoch ms ou null. */
  currentAccessExpiresAt: number | null
  /**
   * Si la transaction est retirée : échéance restaurée depuis les transactions
   * restantes (epoch ms), ou null si l'accès disparaît. Pertinent uniquement
   * quand willRevokeAccess est true.
   */
  restoredExpiresAt: number | null
  accessType: "exam" | "training"
}
```

Dans `getTransactionAccessImpact`, importer `bestCoveringTransaction` depuis `./lib` et remplacer le `return` (lignes 535-539) par :

```ts
const willRevokeAccess = access?.lastTransactionId === transactionId
const restored = willRevokeAccess
  ? await bestCoveringTransaction(db, tx.userId, tx.accessType, transactionId)
  : null

return {
  willRevokeAccess,
  currentAccessExpiresAt: access ? access.expiresAt.getTime() : null,
  restoredExpiresAt: restored ? restored.accessExpiresAt.getTime() : null,
  accessType: tx.accessType,
}
```

- [ ] **Étape 5.2 : Adapter le message du modal d'édition** (`edit-transaction-modal.tsx`, bloc lignes 482-503). Remplacer le `<p>` descriptif par :

```tsx
<p className="mt-1 text-sm text-amber-700 dark:text-amber-300">
  {accessImpact.restoredExpiresAt !== null
    ? `Le remboursement ramènera l'accès ${accessImpact.accessType === "exam" ? "aux examens" : "à l'entraînement"} à son échéance précédente (${formatExpiration(new Date(accessImpact.restoredExpiresAt))}).`
    : `Le remboursement révoquera l'accès ${accessImpact.accessType === "exam" ? "aux examens" : "à l'entraînement"} de l'utilisateur : aucune autre transaction ne le couvre.`}
</p>
```

Ajouter `formatExpiration` à l'import existant `@/lib/format` (ligne 50).

- [ ] **Étape 5.3 : Adapter le dialog de suppression** (`delete-transaction-dialog.tsx`) : même logique — lire le composant, localiser le bloc conditionné par `willRevokeAccess` (autour de la ligne 134) et distinguer les deux cas avec le même texte que 5.2 (« La suppression ramènera… » / « La suppression révoquera… »).

- [ ] **Étape 5.4 : Gate + commit**

```powershell
bun run check && bun run test
git add features/payments/dal.ts components/shared/payments/
git commit -m "C4 (#111): AccessImpact.restoredExpiresAt — avertissements exacts (réduit vs révoqué)"
```

---

## Tâche 6 : C4 — Modal d'édition : `lib/currency` + transition refunded → completed

**Files:**

- Modify: `components/shared/payments/edit-transaction-modal.tsx:78-98` (parseur local), `:450` (condition statut), `:198-202` (toast)

- [ ] **Étape 6.1 : Supprimer le parseur local (lignes 78-98) et importer le canonique**

```ts
import { parseAmountToCents } from "@/lib/currency"
```

(`manual-payment-modal.tsx` importe déjà ce module — parité. Le helper local `centsToDisplayAmount`, lignes 100-106, reste : il est propre à l'affichage de ce modal.)

- [ ] **Étape 6.2 : Permettre le retour `refunded → completed` dans l'UI** — sans ça, le bug #111-1 corrigé côté serveur reste inatteignable pour l'admin. Ligne 450, remplacer :

```tsx
                  {transaction.status === "completed" && (
```

par :

```tsx
                  {(transaction.status === "completed" ||
                    transaction.status === "refunded") && (
```

- [ ] **Étape 6.3 : Toasts exacts** — remplacer les lignes 198-202 par :

```ts
const message =
  data.status === "refunded"
    ? "Transaction remboursée — accès recalculé"
    : data.status === "completed" && transaction.status === "refunded"
      ? "Transaction complétée — accès restauré"
      : "Transaction modifiée avec succès"
toast.success(message)
```

- [ ] **Étape 6.4 : Gate + commit + vérification runtime**

```powershell
bun run check && bun run test
git add components/shared/payments/edit-transaction-modal.tsx
git commit -m "C4 (#111): modal édition — parseur lib/currency, transition refunded→completed, toasts exacts"
```

Vérification runtime (avec `bun dev`, compte admin) : `/admin/transactions` → éditer une transaction manuelle `completed` → passer `refunded` (l'avertissement doit annoncer « ramènera » ou « révoquera » selon le cas) → rouvrir → repasser `completed` → vérifier sur la fiche utilisateur que l'accès est restauré.

---

## Tâche 7 : C6 — Fusion `exam-create-form` / `exam-edit-form` → `exam-form.tsx`

**Files:**

- Create: `app/(admin)/admin/examens/_components/exam-form.tsx`
- Modify: `app/(admin)/admin/examens/creer/page.tsx` (point de montage)
- Modify: `app/(admin)/admin/examens/modifier/[id]/page.tsx` (point de montage)
- Delete: `app/(admin)/admin/examens/creer/_components/exam-create-form.tsx` (714 l.)
- Delete: `app/(admin)/admin/examens/modifier/[id]/_components/exam-edit-form.tsx` (722 l.)

Les deux fichiers partagent `examFormSchema`/`ExamFormValues` (`@/schemas`), les mêmes champs, la même carte Audience, la même section Questions. Base de travail : **partir d'`exam-edit-form.tsx`** (le plus complet) et paramétrer par `mode`.

- [ ] **Étape 7.1 : Créer `exam-form.tsx` avec cette interface**

```tsx
type ExamFormProps =
  | {
      mode: "create"
      candidates: EligibleCandidate[]
      examOptions: ExamPickerOption[]
    }
  | {
      mode: "edit"
      examId: string
      exam: NonNullable<ExamWithQuestions>["exam"]
      questionIds: string[]
      candidates: EligibleCandidate[]
      initialAudience: SelectableUser[]
    }
```

**Décisions d'unification** (chaque divergence recensée → un seul comportement) :

| #   | Divergence                                                 | Décision                                                                                                          |
| --- | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 1   | Badge « durée estimée » (create seul)                      | Gardé pour les DEUX (`Math.ceil((numberOfQuestions * 83) / 60)` min)                                              |
| 2   | Contrôle durée pause : Input (create) vs Slider (edit)     | **Slider** pour les deux (+ `useWatch("pauseDurationMinutes")` pour le badge)                                     |
| 3   | Bloc explicatif pause : Alert riche (create) vs div (edit) | **Alert riche** pour les deux                                                                                     |
| 4   | État vide « pause désactivée » (edit seul)                 | Gardé pour les deux                                                                                               |
| 5   | Toggle pause : fallback                                    | `getDefaultPauseDuration(numberOfQuestions) \|\| 15` pour les deux                                                |
| 6   | `numberOfQuestions` onChange                               | `Number.parseInt(v) \|\| 10` pour les deux                                                                        |
| 7   | `examOptions` du QuestionBrowser                           | Prop passée seulement en `mode === "create"` (prop optionnelle)                                                   |
| 8   | Validation questions à la soumission                       | Logique edit généralisée : `selectedQuestions.length > 0 ? selectedQuestions : form.getValues("questionIds")`     |
| 9   | Mise en page                                               | Grille 2 colonnes de l'edit (`@3xl:grid-cols-2`) + **footer sticky avec compteur** du create, pour les DEUX modes |
| 10  | IDs radios audience                                        | Préfixés : `` `${mode}-audience-subscribers` `` / `` `${mode}-audience-restricted` `` (+ `htmlFor` assortis)      |
| 11  | Format date du picker                                      | `"d MMM yyyy"` pour les deux ; FormDescription de l'edit (« Maximum 14 jours ») conservée                         |

Textes/CTA par mode via un objet local :

```tsx
const MODE_COPY = {
  create: { title: "Créer un examen blanc", submitLabel: "Créer l'examen" },
  edit: {
    title: "Modifier l'examen",
    submitLabel: "Enregistrer les modifications",
  },
} as const
```

`defaultValues`/états initiaux : dérivés de `props.mode === "edit" ? props.exam/questionIds/initialAudience : valeurs vides du create`. `onSubmit` appelle `createExam` ou `updateExam({ id: props.examId, ... })` selon le mode, **via `callAction(() => ...)` de `@/lib/safe-action`** (règle data-layer : jamais d'`await` nu d'une Server Action côté client — les deux fichiers actuels violent la règle, la fusion la rétablit).

- [ ] **Étape 7.2 : Rebrancher les deux pages** — `creer/page.tsx` rend `<ExamForm mode="create" candidates={...} examOptions={...} />` ; `modifier/[id]/page.tsx` rend `<ExamForm mode="edit" examId={...} exam={...} questionIds={...} candidates={...} initialAudience={...} />`. Mêmes données fetchées qu'aujourd'hui.

- [ ] **Étape 7.3 : Supprimer les deux anciens fichiers** puis vérifier :

```powershell
# Ne doit plus rien matcher :
Select-String -Path (Get-ChildItem -Recurse app,components,tests -Include *.ts,*.tsx | % FullName) -Pattern "exam-create-form|exam-edit-form"
bun run check && bun run test
```

Si des tests référencent les anciens chemins, les repointer sur `exam-form.tsx` en préservant leurs assertions.

- [ ] **Étape 7.4 : Vérification visuelle** (`bun dev`, admin) : `/admin/examens/creer` et `/admin/examens/modifier/[id]` — mêmes champs qu'avant, soumission OK dans les deux modes.

- [ ] **Étape 7.5 : Commit**

```powershell
git add -A
git commit -m "C6 (#113): fusion exam-create-form/exam-edit-form → exam-form (mode create|edit) + callAction"
```

---

## Tâche 8 : C6 — StatCard animé unique (3 copies → 1)

**Files:**

- Create: `components/admin/animated-stat-card.tsx`
- Modify: `app/(admin)/admin/utilisateurs/_components/users-stats-row.tsx` (composant local lignes 27-186 → import)
- Modify: `app/(admin)/admin/examens/_components/exams-stats-row.tsx` (lignes 21-145 → import)
- Modify: `app/(admin)/admin/questions/_components/questions-stats-row.tsx` (lignes 21-145 → import)

Périmètre : les **3 copies `motion.div` quasi identiques** (famille B de l'inventaire). NE PAS toucher : `components/admin/stat-card.tsx` (famille A, déjà partagé + testé), `exam-stat-card.tsx` et `transaction-stats.tsx` (famille C, designs réellement distincts).

- [ ] **Étape 8.1 : Créer `components/admin/animated-stat-card.tsx`** en déplaçant tel quel le composant de `users-stats-row.tsx` (la version la plus complète : lignes 27-186, `colorVariants` inclus) avec :

```tsx
export type AnimatedStatCardColor =
  | "emerald"
  | "blue"
  | "amber"
  | "teal"
  | "slate"

export type AnimatedStatCardProps = {
  label: string
  value: string | number
  icon: ReactNode
  color: AnimatedStatCardColor
  delay?: number
  subtitle?: string
  trend?: { value: number; isPositive: boolean }
}

export const AnimatedStatCard = (props: AnimatedStatCardProps) => {
  /* markup users-stats-row inchangé */
}
```

Union `color` = les 5 couleurs (users avait slate, questions avait slate, exams non — l'union couvre tout). Le bloc `trend` (lignes 125-149 d'users) reste conditionnel (`trend &&`) — les rows exams/questions ne le passent simplement pas. Conserver le pattern « subtitle réserve l'espace » (règle admin-ui : hauteur uniforme) et le skeleton `h-39 animate-pulse` (l'exporter aussi : `AnimatedStatCardSkeleton`).

- [ ] **Étape 8.2 : Remplacer les 3 définitions locales** par `import { AnimatedStatCard } from "@/components/admin/animated-stat-card"` — chaque row garde sa config (labels, icônes, couleurs) et son markup d'appel. Supprimer les `colorVariants` locaux devenus morts.

- [ ] **Étape 8.3 : Gate + vérification visuelle + commit**

```powershell
bun run check && bun run test
```

Visuel : `/admin/utilisateurs`, `/admin/examens`, `/admin/questions` — cartes identiques (couleurs, anim, trend sur users).

```powershell
git add -A
git commit -m "C6 (#113): AnimatedStatCard unique remplace les 3 copies stats-row"
```

---

## Tâche 9 : C6 — Découpage `features/exams/dal.ts` (1518 lignes)

**Files:**

- Create: `features/exams/dal.shared.ts`, `features/exams/dal.student.ts`, `features/exams/dal.admin.ts`
- Modify: `features/exams/dal.ts` → devient un baril de ré-export (compat imports existants)

**PUR DÉPLACEMENT** — aucun changement de logique, de garde ou de signature.

- [ ] **Étape 9.1 : `dal.shared.ts`** (interne + types transverses) : helpers `groupImages` (l.47), `fetchImages` (l.63), `countQuestionsByExam` (l.125), `getOpenExamLockedQuestionIds` (l.730), `getOpenExamQuestionIds` (l.761) ; types `ExamImageView` (l.41), `ExamQuestionView` (l.89). `import "server-only"` en tête.

- [ ] **Étape 9.2 : `dal.student.ts`** (12 fonctions, garde `getCurrentSession`) : `getExamsWithParticipation`, `getExamWithQuestions`, `getExamSession`, `getExamAnswersForParticipation`, `getParticipantExamResults`, `getExamQuestionExplanations`, `getExamSubmissionSummary`, `getExamLeaderboard`, `getMyDashboardStats`, `getMyRecentExams`, `getMyScoreHistory`, `getMyAvailableExams` + leurs types associés. (`getExamWithQuestions` et `getParticipantExamResults` gardent leur branche `isAdmin` interne — hybrides assumés côté étudiant.)

- [ ] **Étape 9.3 : `dal.admin.ts`** (6 fonctions, garde `requireRole(["admin"])`) : `getAllExamsAdmin`, `getExamsForPicker`, `getExamsStats`, `getActiveExamAccessCount`, `getEligibleExamCandidates`, `getExamAudience` + types associés.

- [ ] **Étape 9.4 : `dal.ts` devient le baril**

```ts
import "server-only"

export * from "./dal.admin"
export * from "./dal.shared"
export * from "./dal.student"
```

Aucun import existant (`@/features/exams/dal`) ne casse — y compris les `import type` côté client (module `server-only` effacé à la compilation).

- [ ] **Étape 9.5 : Gate + commit**

```powershell
bun run check && bun run test && bun run test:integration
git add features/exams/
git commit -m "C6 (#113): split features/exams/dal.ts → student/admin/shared (pur déplacement, baril compat)"
```

(`test:integration` obligatoire ici : les tests d'intégration exams importent la DAL réelle.)

---

## Tâche 10 : C6 — Consolidation dates dans `lib/format.ts`

**Files:**

- Modify: `lib/format.ts` (4 helpers manquants)
- Modify: ~20 sites listés ci-dessous

Règle : **conserver le format visible à l'identique** — on remplace l'implémentation, pas le rendu. `lib/format.ts` a déjà : `formatCurrency`, `formatExpiration` (`d MMMM yyyy`), `formatTimeRemaining` (distance relative fr), `formatShortDate` (`dd/MM/yyyy`), `formatDateTime` (`d MMM yyyy à HH:mm`), `formatTimeOnly` (`HH:mm`).

- [ ] **Étape 10.1 : Ajouter les 4 helpers manquants** (même style que le fichier existant, date-fns + locale `fr`) :

```ts
/** « 3 juil. 2026 » — listes/cards admin. */
export const formatMediumDate = (d: Date | number | string) =>
  format(new Date(d), "d MMM yyyy", { locale: fr })

/** « 3 juillet 2026 à 14:05 » — panneaux de détail. */
export const formatLongDateTime = (d: Date | number | string) =>
  format(new Date(d), "d MMMM yyyy 'à' HH:mm", { locale: fr })

/** « 3 juillet 2026 à 14:05 » (variante PPP) — détails examen. */
export const formatFullDateTime = (d: Date | number | string) =>
  format(new Date(d), "PPP 'à' HH:mm", { locale: fr })

/** « 03/07/2026, 14:05 » — lignes compactes (leaderboard, tables). */
export const formatCompactDateTime = (d: Date | number | string) =>
  format(new Date(d), "Pp", { locale: fr })
```

- [ ] **Étape 10.2 : Remplacer les sites inline** (mapping issu de l'inventaire — vérifier chaque site au passage, le rendu ne doit pas changer) :

| Sites                                                                                                                                                                                | Helper                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| `users-table.tsx:121`, `question-browser-table.tsx:121`, `activity-feed.tsx:70`, `training-history-section.tsx:213`, `delete-session-dialog.tsx:132`, `recent-activity-feed.tsx:132` | `formatTimeRemaining` (existant)                                                                |
| `exam-card.tsx:86,91`, `exam-side-panel.tsx:244,258`, `eligible-candidates-section.tsx:194`, `user-side-panel.tsx:214`, `examen-blanc-client.tsx:57`                                 | `formatMediumDate`                                                                              |
| `user-side-panel.tsx:368`, `user-info-card.tsx:96`, `profile-header.tsx:21`, `training-score-chart-content.tsx:81`, `score-evolution-chart-content.tsx:53`                           | `formatExpiration` (existant)                                                                   |
| `question-preview-panel.tsx:279`, `question-side-panel.tsx:337`                                                                                                                      | `formatLongDateTime`                                                                            |
| `exam-details.tsx:69,80`, `examen-blanc-client.tsx:54`                                                                                                                               | `formatFullDateTime`                                                                            |
| `exam-side-panel.tsx:249,261`                                                                                                                                                        | `formatTimeOnly` (existant)                                                                     |
| `user-table-row.tsx:40`, `exam-leaderboard.tsx:149`                                                                                                                                  | `formatCompactDateTime`                                                                         |
| `users-table.tsx:263`, `question-browser-table.tsx:298` (`toLocaleDateString` — risque hydration)                                                                                    | remplacer par le helper date-fns dont le rendu correspond au format actuel (vérifier sur place) |

Laisser inline les one-off assumés : `admin-dashboard-client.tsx:64` (`EEEE d MMMM yyyy`), `revenue-chart-content.tsx:59` (`d MMM` sur axe), `users-filter-bar.tsx:86-87` (labels de plage), `features/users/dal.ts:116` (Intl serveur volontaire). Les `toLocaleDateString("fr-FR")` des exports (`export-users-button.tsx:22`, `export-questions-button.tsx:148`) sont traités en Tâche 11.

- [ ] **Étape 10.3 : Gate + commit**

```powershell
bun run check && bun run test
git add -A
git commit -m "C6 (#113): consolidation dates — 4 helpers lib/format + remplacement des sites inline"
```

---

## Tâche 11 : C6 — `lib/export.ts` (XLSX/CSV dédupliqués)

**Files:**

- Create: `lib/export.ts`
- Modify: `components/admin/export-users-button.tsx` (157 l.)
- Modify: `app/(admin)/admin/questions/_components/export-questions-button.tsx` (253 l.)

- [ ] **Étape 11.1 : Créer `lib/export.ts`** (utilitaire client — pas de `server-only`) :

```ts
/**
 * Helpers d'export côté client (XLSX via SheetJS, CSV manuel + BOM UTF-8).
 * Consolidé depuis export-users-button et export-questions-button (C6, #113).
 */
import { utils, writeFile } from "xlsx"

/** Déclenche le téléchargement navigateur d'un Blob puis libère l'URL. */
export const downloadBlob = (blob: Blob, filename: string): void => {
  const url = URL.createObjectURL(blob)
  const a = document.createElement("a")
  a.href = url
  a.download = filename
  document.body.appendChild(a)
  a.click()
  document.body.removeChild(a)
  URL.revokeObjectURL(url)
}

/** `base-2026-07-16.ext` (date locale ISO courte). */
export const timestampedFilename = (base: string, ext: string): string =>
  `${base}-${new Date().toISOString().split("T")[0]}.${ext}`

export const exportRowsToXlsx = (
  rows: Record<string, unknown>[],
  opts: { sheetName: string; filename: string; colWidths?: number[] },
): void => {
  const worksheet = utils.json_to_sheet(rows)
  if (opts.colWidths)
    worksheet["!cols"] = opts.colWidths.map((wch) => ({ wch }))
  const workbook = utils.book_new()
  utils.book_append_sheet(workbook, worksheet, opts.sheetName)
  writeFile(workbook, opts.filename)
}

/** CSV : échappement `"`, BOM UTF-8 (Excel), séparateur paramétrable. */
export const exportRowsToCsv = (
  rows: Record<string, unknown>[],
  opts: { filename: string; separator: ";" | "," },
): void => {
  if (rows.length === 0) return
  const headers = Object.keys(rows[0])
  const escape = (v: unknown) => `"${String(v ?? "").replace(/"/g, '""')}"`
  const lines = [
    headers.map(escape).join(opts.separator),
    ...rows.map((r) => headers.map((h) => escape(r[h])).join(opts.separator)),
  ]
  const blob = new Blob(["\uFEFF" + lines.join("\n")], {
    type: "text/csv;charset=utf-8;",
  })
  downloadBlob(blob, opts.filename)
}
```

- [ ] **Étape 11.2 : Refactorer les deux boutons** pour n'y garder que : préparation des lignes (mapping colonnes), UI (`DropdownMenu`, toasts, spinner), et l'appel aux helpers. **Avant de brancher, relever le séparateur CSV réel de chaque bouton** (users: lignes 80-118 ; questions: lignes 73-122) et le passer explicitement — le rendu des fichiers exportés ne doit pas changer. `exportAsJSON` (questions, l.124-130) reste local mais utilise `downloadBlob`. Les dates de cellules utilisent `formatShortDate` de `lib/format` (rendu `dd/MM/yyyy`, identique au `toLocaleDateString("fr-FR")` actuel).

- [ ] **Étape 11.3 : Vérification fonctionnelle + gate + commit**

`bun dev` (admin) : exporter users en XLSX et CSV, questions en XLSX/CSV/JSON — ouvrir les fichiers, contenu identique à avant (accents inclus — BOM).

```powershell
bun run check && bun run test
git add -A
git commit -m "C6 (#113): lib/export.ts — XLSX/CSV/download dédupliqués des deux boutons d'export"
```

---

## Tâche 12 : C7 — Stats marketing rendues côté serveur

**Files:**

- Modify: `app/(marketing)/page.tsx`, `tarifs/page.tsx`, `domaines/page.tsx`, `a-propos/page.tsx`, `evaluation/page.tsx` (fetch serveur + prop)
- Modify (8 composants, prop au lieu du hook) : `_components/home-landing.tsx`, `tarifs/_components/pricing-header.tsx`, `domaines/_components/domains-grid.tsx`, `domaines/_components/domaines-stats.tsx`, `a-propos/_components/about-story.tsx`, `a-propos/_components/about-stats.tsx`, `a-propos/_components/a-propos-page-client.tsx`, `evaluation/_components/evaluation-stats.tsx`
- Delete: `hooks/useMarketingStats.ts`
- Delete (si existant) : `features/marketing/actions.ts` (`loadMarketingStats` n'aura plus d'appelant — vérifier par grep avant)

État des lieux : la DAL est déjà propre (`features/marketing/dal.ts` — `getMarketingStats` en `cache()` + `server-only`), mais les 8 composants fetchent encore côté client via `hooks/useMarketingStats.ts` (waterfall + skeleton → CLS + contenu invisible au SEO). L'item de l'issue est donc **partiellement** traité par #84/#85 : il reste la bascule RSC.

- [ ] **Étape 12.1 : Lire la doc Next avant de toucher au cache** — `node_modules/next/dist/docs/` (fichier sur l'ISR/revalidate) pour confirmer la syntaxe `export const revalidate` en Next 16 (règle projet : docs avant code Next).

- [ ] **Étape 12.2 : Basculer page par page** — pattern (exemple `tarifs/page.tsx`) :

```tsx
import { getMarketingStats } from "@/features/marketing/dal"

// Stats quasi statiques : page régénérée au plus toutes les heures (ISR).
export const revalidate = 3600

export default async function TarifsPage() {
  const stats = await getMarketingStats()
  return <TarifsPageClient stats={stats} />
}
```

Et dans chaque composant consommateur, remplacer :

```tsx
const { stats } = useMarketingStats()
```

par une prop typée :

```tsx
import type { MarketingStats } from "@/features/marketing/dal"

type Props = { stats: MarketingStats /* + props existantes */ }
```

(`import type` d'un module `server-only` : autorisé, effacé à la compilation.) Les composants restent `"use client"` s'ils utilisent `motion` — seule la donnée arrive en prop. Supprimer les états de skeleton liés au chargement des stats (la donnée est là au premier rendu). Quand un composant est imbriqué (ex. `a-propos-page-client.tsx` → `about-story`/`about-stats`), la prop descend en cascade depuis la page.

- [ ] **Étape 12.3 : Supprimer le hook** puis vérifier :

```powershell
Select-String -Path (Get-ChildItem -Recurse app,components,hooks,features -Include *.ts,*.tsx | % FullName) -Pattern "useMarketingStats|loadMarketingStats"
```

Attendu : 0 match applicatif (sinon, un consommateur a été oublié). Supprimer `features/marketing/actions.ts` si `loadMarketingStats` n'a plus d'appelant.

- [ ] **Étape 12.4 : Gate + vérification + commit**

`bun dev` : accueil, /tarifs, /domaines, /a-propos, /evaluation — les chiffres s'affichent au premier rendu (voir le HTML source : les stats doivent y être).

```powershell
bun run check && bun run test
git add -A
git commit -m "C7 (#114): stats marketing en Server Components + ISR 1h — fin des 8 fetchs client"
```

---

## Tâche 13 : C7 — a11y quiz + sr-only français

**Files:**

- Modify: `components/quiz/question-card/answer-option.tsx:102-117`
- Modify: `components/quiz/question-card/index.tsx:450` (conteneur des options)
- Modify: `components/ui/sheet.tsx:72`, `components/ui/sidebar.tsx:276`

- [ ] **Étape 13.1 : Options de réponse — pattern « toggle button »** (choisi plutôt que `role="radio"` : le pattern radio ARIA exige une navigation aux flèches + roving tabindex, hors périmètre ; `aria-pressed` sur des boutons est correct et déjà utilisé dans la carte pour le flag, index.tsx:306-311). Sur le `<motion.button>` (answer-option.tsx:102-117), ajouter :

```tsx
              aria-pressed={state === "selected"}
```

Les `data-testid={`answer-option-${index}`}` et `data-selected` existants NE BOUGENT PAS (contrat e2e).

- [ ] **Étape 13.2 : Groupe des options** — sur le conteneur (index.tsx:450), ajouter :

```tsx
              role="group"
              aria-label="Choix de réponse"
```

- [ ] **Étape 13.3 : sr-only anglais → français** (les deux seuls restants, composants shadcn stock) :
  - `components/ui/sheet.tsx:72` : `<span className="sr-only">Close</span>` → `Fermer`
  - `components/ui/sidebar.tsx:276` : `<span className="sr-only">Toggle Sidebar</span>` → `Basculer la barre latérale`

- [ ] **Étape 13.4 : Gate + e2e ciblés + commit**

```powershell
bun run check && bun run test
bun run test:e2e   # les specs quiz pilotent answer-option-* — non-régression sélecteurs
git add -A
git commit -m "C7 (#114): a11y quiz (aria-pressed + groupe libellé) et sr-only en français"
```

---

## Tâche 14 : C7 — `<Link><Button>` → `<Button asChild>` (37 sites) + CTA tarifs mort

**Files:** les 19 fichiers de l'inventaire (liste ci-dessous) + `app/(marketing)/_components/home-landing.tsx:419`.

- [ ] **Étape 14.1 : Réparer le CTA mort** (`home-landing.tsx:419-424` — bouton « Voir les tarifs » sans lien ni handler, contrairement à son voisin « Inscription gratuite »). Appliquer directement le pattern cible :

```tsx
<Button
  asChild
  variant="outline"
  className="glass-card-dark h-auto rounded-2xl border-2 border-white/30 px-12 py-4 text-lg font-semibold text-white ..."
>
  <Link href="/tarifs">Voir les tarifs</Link>
</Button>
```

(className inchangé ; `/tarifs` = route de `constants/index.tsx:120` et des deux autres CTA « Voir les tarifs » fonctionnels.)

- [ ] **Étape 14.2 : Convertir les 37 imbrications** — transformation mécanique, le `className`/`variant`/`size` restent sur le `Button`, le `href`/`target` restent sur le `Link` :

```tsx
// AVANT (HTML invalide : <a><button>)
<Link href="/x"><Button variant="outline">Texte</Button></Link>
// APRÈS
<Button asChild variant="outline"><Link href="/x">Texte</Link></Button>
```

Sites (fichier : lignes) : `components/shared/payments/upgrade-prompt.tsx:177` · `components/marketing-header/mobile-menu.tsx:172,205,217` · `components/marketing-header/index.tsx:209,217` · `components/marketing/domain-card.tsx:95` · `app/(marketing)/_components/start-quiz-cta.tsx:10` · `home-landing.tsx:119,128,315,377,413` · `tarifs/_components/tarifs-page-client.tsx:124,133` · `a-propos/_components/about-cta.tsx:25,34` · `paiement/_components/payment-success-client.tsx:303,312,364,403,411` · `app/(auth)/connexion/page.tsx:100` · `domaines/_components/domaines-cta.tsx:19` · `admin/utilisateurs/[id]/user-detail-client.tsx:98` et `page.tsx:28,42` · `admin/questions/_components/questions-manager.tsx:85`, `question-side-panel.tsx:359`, `question-form-page.tsx:166,728` · `admin/utilisateurs/_components/user-side-panel.tsx:423,449` · `profil/_components/profile-subscription-card.tsx:174` · `abonnements/_components/abonnements-client.tsx:185,197,383`.

Attention aux cas `disabled` sur le Button : `asChild` + Link laisse le lien cliquable — si un site a un état disabled, le préserver en conditionnant le rendu (Link seulement si actif). Vérifier au passage (aucun connu à l'inventaire, mais re-checker en éditant).

- [ ] **Étape 14.3 : Vérifier qu'il n'en reste plus**

```powershell
# (rg via Grep tool : pattern multiline "<Link[^>]*>\s*<Button" et l'inverse) — attendu : 0
bun run check && bun run test
```

- [ ] **Étape 14.4 : Commit**

```powershell
git add -A
git commit -m "C7 (#114): Button asChild sur 37 imbrications Link>Button + CTA « Voir les tarifs » réparé"
```

---

## Tâche 15 : C7 — Filtres transactions admin dérivés de l'URL

**Files:**

- Modify: `app/(admin)/admin/transactions/page.tsx`
- Modify: `app/(admin)/admin/transactions/_components/transactions-manager.tsx:50-103`

Cible : `type`/`status` (filtres serveur) vivent dans l'URL — deep-linking + rechargement sans perte (convention « URL state » ; cf. `app/(admin)/admin/examens/page.tsx` pour le pattern maison). `searchQuery` (filtre client en mémoire sur la page chargée) reste en `useState` local.

- [ ] **Étape 15.1 : `page.tsx` lit les `searchParams` et filtre le fetch initial**

```tsx
const TYPES = ["stripe", "manual"] as const
const STATUSES = ["pending", "completed", "failed", "refunded"] as const

export default async function AdminTransactionsPage({
  searchParams,
}: {
  searchParams: Promise<{ type?: string; status?: string }>
}) {
  const params = await searchParams
  const type = TYPES.find((t) => t === params.type)
  const status = STATUSES.find((s) => s === params.status)

  const [initialPage, stats, products, users] = await Promise.all([
    getAllTransactions({ limit: 20, type, status }),
    getTransactionStats(),
    getAvailableProducts(),
    getSelectableUsers(),
  ])

  return (
    <div className="flex flex-col gap-6 p-4 md:gap-8 lg:p-6">
      <TransactionsManager
        initialItems={initialPage.items}
        initialCursor={initialPage.nextCursor}
        initialStats={stats}
        initialType={type}
        initialStatus={status}
        products={products}
        users={users}
      />
    </div>
  )
}
```

(En Next 16, `searchParams` est une Promise — confirmer dans `node_modules/next/dist/docs/` au moment de coder.)

- [ ] **Étape 15.2 : `transactions-manager.tsx`** — initialiser depuis les props (`useState(initialType ?? "all")`, idem status) et, dans `handleTypeChange`/`handleStatusChange`/`handleClearFilters` (lignes 88-103), synchroniser l'URL en plus du fetch existant :

```tsx
const router = useRouter()
const pathname = usePathname()

const syncUrl = (type: TypeFilter, status: StatusFilter) => {
  const qs = new URLSearchParams()
  if (type !== "all") qs.set("type", type)
  if (status !== "all") qs.set("status", status)
  router.replace(qs.size ? `${pathname}?${qs}` : pathname, { scroll: false })
}
```

Appeler `syncUrl` dans chaque handler à côté d'`applyFilters` (le mécanisme `startTransition` + `loadAdminTransactions` existant reste le canal de données ; l'URL devient la source au chargement). Pas de `useEffect` de synchronisation (règle ESLint `set-state-in-effect`).

- [ ] **Étape 15.3 : Vérification + gate + commit**

`bun dev` : appliquer type=manual + status=refunded → recharger la page → filtres et liste conservés ; partager l'URL dans un autre onglet → même état.

```powershell
bun run check && bun run test
git add -A
git commit -m "C7 (#114): filtres transactions admin dérivés de l'URL (deep-linking + rechargement)"
```

---

## Tâche 16 : C7 — Metadata, loading.tsx, next/image

**Files:**

- Modify: `app/(auth)/connexion/page.tsx`, `app/(auth)/inscription/page.tsx` (metadata directe — pages serveur)
- Create: `app/(auth)/mot-de-passe-oublie/layout.tsx`, `app/(auth)/reinitialiser-mot-de-passe/layout.tsx`, `app/(marketing)/evaluation/quiz/layout.tsx`, `app/(dashboard)/tableau-de-bord/bienvenue/layout.tsx` (pages `"use client"` → metadata via layout)
- Modify: les 12 pages serveur de `app/(dashboard)/**` (metadata directe)
- Create: `loading.tsx` par sous-segment dashboard (liste ci-dessous)
- Modify: `components/quiz/question-card/index.tsx:125-131` (`<img>` → `next/image`)

- [ ] **Étape 16.1 : Metadata** — le root layout a déjà `template: "%s | NOMAQbanq"` (app/layout.tsx:30) : ne fournir que des titres courts.

Pages serveur — ajouter en tête :

```tsx
import type { Metadata } from "next"

export const metadata: Metadata = { title: "Connexion" }
```

Titres : Connexion, Inscription (auth) ; Tableau de bord, Entraînement, Session d'entraînement, Résultats d'entraînement, Examens blancs, Examen blanc, Évaluation, Résultats, Soumission, Paiement réussi, Profil, Abonnements (dashboard — adapter au segment ; les pages dynamiques `[examId]`/`[sessionId]` prennent le titre générique du segment, pas de `generateMetadata` fetché : YAGNI).

Pages client (4) — créer un `layout.tsx` minimal dans le segment :

```tsx
import type { Metadata } from "next"

export const metadata: Metadata = { title: "Mot de passe oublié" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
```

(Titres : Mot de passe oublié, Réinitialiser le mot de passe, Quiz d'évaluation, Bienvenue.)

- [ ] **Étape 16.2 : `loading.tsx` par sous-segment dashboard** — modèle : l'existant `app/(dashboard)/tableau-de-bord/loading.tsx` (le lire et décliner). Créer dans : `tableau-de-bord/entrainement/`, `tableau-de-bord/examen-blanc/`, `tableau-de-bord/profil/`, `tableau-de-bord/abonnements/`, `app/(marketing)/evaluation/`. (Pas de loading pour `(auth)` : pages statiques légères.)

- [ ] **Étape 16.3 : Images d'explication en `next/image`** — `components/quiz/question-card/index.tsx:125-131`, remplacer le `<img>` (et son `eslint-disable @next/next/no-img-element`) par :

```tsx
<Image
  src={img.url}
  alt="Image d'explication"
  width={1200}
  height={800}
  className="h-auto w-full rounded-lg border"
  sizes="(max-width: 768px) 100vw, 700px"
/>
```

(`import Image from "next/image"` ; conserver les classes existantes du `<img>` actuel en les fusionnant ; les hosts CDN sont déjà autorisés dans next.config.ts. `width/height` = ratio indicatif, `h-auto w-full` préserve le ratio réel.)

- [ ] **Étape 16.4 : Gate + commit**

```powershell
bun run check && bun run test
git add -A
git commit -m "C7 (#114): metadata (auth/dashboard/quiz), loading.tsx par sous-segment, image d'explication en next/image"
```

---

## Tâche 17 : Gates finaux, vérification bout en bout, livraison

- [ ] **Étape 17.1 : Suite complète**

```powershell
bun run check
bun run test
bun run test:integration
bun run test:e2e
```

Attendu : tout vert. Toute régression e2e sur les sélecteurs quiz = retour Tâche 13/14.

- [ ] **Étape 17.2 : Vérification runtime finale** (`bun dev`) — parcours : (1) admin transactions : édition/refund/re-complétion/suppression avec les nouveaux avertissements ; (2) admin examens : créer + modifier via le formulaire fusionné ; (3) exports users/questions ; (4) pages marketing : stats dans le HTML source ; (5) quiz d'évaluation : options répondables, CTA tarifs de l'accueil.

- [ ] **Étape 17.3 : Marquer l'item « stats marketing » comme partiellement pré-résolu dans l'issue #114** (commentaire GitHub à faire par Samuel au moment de la PR — pas de gh CLI sur ce poste).

- [ ] **Étape 17.4 : Push + PR**

```powershell
git push -u origin campaign/audit-2026-07-c4-c7
```

La PR est ouverte manuellement (pas de gh CLI). Titre : `Campagne audit 2026-07 : C4 fiabilité paiements + C5 nettoyage + C6 dédup + C7 frontend`. Corps : une section par campagne (résumé des commits), la note de sémantique `recomputeAccess` (restauration du max, pas de re-simulation), et en pied :

```
Closes #111
Closes #112
Closes #113
Closes #114

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

---

## Hors périmètre (assumé)

- Re-simulation du cumul « comme si la transaction n'avait jamais existé » (on restaure le max des snapshots — documenté dans le JSDoc).
- Pattern ARIA radio complet (roving tabindex + flèches) sur les options de quiz — `aria-pressed` retenu.
- `generateMetadata` dynamique par examen/session (titres génériques suffisants).
- Migration `searchQuery` (filtre client) vers l'URL — seuls `type`/`status` (filtres serveur) y passent.
- Familles A et C de StatCard (designs réellement distincts, famille A déjà testée).
